import styled from 'styled-components/native';
import Colors from '../utils/Colors';

export const Text = styled.Text`
  color: ${Colors.mainGray};
  font-size: 17px;
  text-align: center;
`;
export const TextConsent = styled.Text`
  color: ${Colors.mainGray};
  font-size: 15px;
  text-align: left;
`;

export const TextBlue = styled.Text`
  color: ${Colors.mainBlue};
  font-size: 20px;
  text-align: center;
`;

export const TextRed = styled.Text`
  color: ${Colors.red};
  font-size: 15px;
  text-align: left;
`;

export const TextWhite = styled.Text`
  color: ${Colors.white};
  font-size: 17px;
  text-align: left;
`;

export const TextMainBlue = styled.Text`
  color: ${Colors.mainBlue};
  font-size: 17px;
  text-align: left;
`;

export const TextBlack = styled.Text`
  color: black;
  font-size: 17px;
  text-align: left;
`;
